package com.example.pacrypto.util


enum class Refresh {
    FORCE, NORMAL
}

enum class FabState {
    SHOW, HIDE
}

enum class SearchType {
    TICKER, NAME
}

enum class SearchRate {
    USD, RUB
}